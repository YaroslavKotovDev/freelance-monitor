# Deprecated Document

This file is kept only to avoid broken links.

It is no longer a source of truth.

Use instead:

- `docs/architecture.md`

The previous n8n-oriented blueprint was intentionally retired because the MVP now follows a simpler custom TypeScript worker architecture.
