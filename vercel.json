{
  "buildCommand": "npm install --prefix admin && npm run build --prefix admin",
  "outputDirectory": "public",
  "functions": {
    "api/webhook.ts": {
      "memory": 256,
      "maxDuration": 30
    }
  }
}
